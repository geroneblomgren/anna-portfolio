---
phase: 04-atmospheric-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/globals.css
  - src/components/frontend/IntroAnimation.tsx
  - src/app/(frontend)/about/page.tsx
  - src/app/(frontend)/contact/page.tsx
autonomous: false
requirements:
  - ATMO-03

must_haves:
  truths:
    - "Bodoni Moda headings use aggressive letter-spacing — visibly more commanding than v1.0"
    - "Size contrast between headings and body text is dramatic (minimum 3:1 ratio)"
    - "Strategic italic is used on secondary lines, not primary names"
    - "RichText h2 elements in About page have the same tracking treatment as direct h2 elements"
    - "Typography feels consistent across all pages — gallery intro, about, contact"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand tracking tokens in @theme"
      contains: "--tracking-brand"
    - path: "src/components/frontend/IntroAnimation.tsx"
      provides: "Upgraded intro heading typography"
      contains: "tracking-brand"
    - path: "src/app/(frontend)/about/page.tsx"
      provides: "Upgraded about page heading typography"
      contains: "tracking-brand"
    - path: "src/app/(frontend)/contact/page.tsx"
      provides: "Upgraded contact heading typography"
      contains: "tracking-brand"
  key_links:
    - from: "src/app/globals.css"
      to: "all heading components"
      via: "@theme tracking tokens"
      pattern: "--tracking-brand"
    - from: "src/app/(frontend)/about/page.tsx"
      to: "RichText h2 selectors"
      via: "[&_h2]: arbitrary variant chain"
      pattern: "\\[&_h2\\]:tracking-brand"
---

<objective>
Add brand typography tokens and upgrade all headings across every page to aggressive, commanding letter-spacing with dramatic size contrast and strategic italic use.

Purpose: Typography is the cheapest lever for visual attitude. Bodoni Moda with wide tracking and dramatic scale transforms "polished but safe" headings into "dark and commanding" without any motion or JS.

Output: @theme tracking tokens in globals.css, upgraded headings in IntroAnimation, about, and contact pages.
</objective>

<execution_context>
@C:/Users/geron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/geron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-atmospheric-foundation/04-RESEARCH.md
@.planning/phases/04-atmospheric-foundation/04-01-SUMMARY.md

@src/app/globals.css
@src/components/frontend/IntroAnimation.tsx
@src/app/(frontend)/about/page.tsx
@src/app/(frontend)/contact/page.tsx

<interfaces>
<!-- From globals.css @theme block (after Plan 01): -->
Existing tokens: --color-bg, --color-surface, --color-border, --color-text-heading, --color-text-body, --color-text-muted, --color-accent, etc.
Font tokens: --font-heading (Bodoni Moda), --font-body (DM Sans)
New tokens to add: --tracking-brand-tight, --tracking-brand-wide, --tracking-brand-widest

<!-- From IntroAnimation.tsx (after Plan 01 refactor): -->
The component no longer has AnimatePresence. It has a motion.h1 "Anna Blomgren" and motion.p "Artist & Illustrator" that need typography upgrades.

<!-- From about/page.tsx: -->
- Direct h2: `<h2 className="font-heading text-text-heading text-2xl mb-4">Artist Statement</h2>` (line 61)
- Direct h2: `<h2 className="font-heading text-text-heading text-2xl mb-4">Contact</h2>` (line 73)
- RichText container: `[&_h2]:text-2xl [&_h2]:font-heading` chain on bioText div (line 53)

<!-- From contact/page.tsx: -->
- h1: `<h1 className="font-heading text-text-heading text-4xl md:text-5xl mb-4">Get in Touch</h1>` (line 16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typography tokens and upgrade all headings</name>
  <files>src/app/globals.css, src/components/frontend/IntroAnimation.tsx, src/app/(frontend)/about/page.tsx, src/app/(frontend)/contact/page.tsx</files>
  <action>
**Step 1 — Add tracking tokens to @theme in globals.css:**

Inside the existing `@theme { }` block, add after the font tokens:

```css
/* Typography attitude — commanding letter-spacing */
--tracking-brand-tight: -0.02em;
--tracking-brand-wide: 0.12em;
--tracking-brand-widest: 0.2em;
```

**Step 2 — Upgrade IntroAnimation.tsx headings:**

Find the `motion.h1` for "Anna Blomgren" (currently `text-5xl md:text-7xl`):
- Change to: `text-5xl md:text-8xl tracking-brand-wide`
- The size bump from `md:text-7xl` to `md:text-8xl` creates stronger hierarchy

Find the `motion.p` for "Artist & Illustrator" (currently `text-xl italic mt-3`):
- Change to: `text-xl italic tracking-brand-widest mt-3 uppercase`
- Italic stays on the secondary line (strategic italic rule)
- `uppercase` + `tracking-brand-widest` (0.2em) creates a spaced-out label effect

**Step 3 — Upgrade contact/page.tsx heading:**

Find `h1` "Get in Touch" (currently `text-4xl md:text-5xl`):
- Change to: `text-5xl md:text-7xl tracking-brand-wide`
- Size bump from `text-4xl` to `text-5xl` (mobile) and `md:text-5xl` to `md:text-7xl` (desktop) widens the contrast ratio against body text to well above 3:1

**Step 4 — Upgrade about/page.tsx headings:**

a) Direct h2 "Artist Statement" (line 61, currently `text-2xl`):
   - Change to: `text-3xl md:text-4xl tracking-brand-wide`

b) Direct h2 "Contact" (line 73, currently `text-2xl`):
   - Change to: `text-3xl md:text-4xl tracking-brand-wide`

c) RichText bioText container (line 53, the long className string):
   - Find `[&_h2]:text-2xl` and change to `[&_h2]:text-3xl md:[&_h2]:text-4xl`
   - Find `[&_h2]:font-heading` and add `[&_h2]:tracking-brand-wide` next to it
   - Find `[&_h3]:text-xl` and change to `[&_h3]:text-2xl`
   - Add `[&_h3]:tracking-brand-wide` next to `[&_h3]:font-heading`

Do NOT italicize primary headings. Italic is reserved for the intro subtitle and blockquote content (already italic).
  </action>
  <verify>
    <automated>cd "C:/Users/geron/OneDrive/Desktop/AI Learning/AI Personal Projects/Anna Portfolio" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All four files updated. globals.css has three --tracking-brand-* tokens. IntroAnimation h1 is text-8xl with tracking-brand-wide. Contact h1 is text-7xl with tracking-brand-wide. About h2 elements (direct and RichText) have tracking-brand-wide. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of full Phase 4 atmosphere</name>
  <action>
Human verifies the complete atmospheric foundation: film grain overlay, vignette overlay, AnimatePresence refactor behavior, and commanding typography across all pages.
  </action>
  <what-built>Full Phase 4 atmospheric foundation: film grain overlay, vignette overlay, AnimatePresence refactor, and commanding typography across all pages.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Clear localStorage (`localStorage.removeItem('intro-seen')` in console) and refresh
    3. Verify intro animation plays with the larger "Anna Blomgren" heading and spaced-out "ARTIST & ILLUSTRATOR" subtitle
    4. After intro completes (or click Skip), verify:
       - Film grain texture is subtly visible across the gallery page (look for slight noise in dark areas)
       - Screen edges are slightly darker (vignette effect)
       - Gallery thumbnails are still clickable (grain/vignette do not block clicks)
    5. Navigate to /about — verify h2 headings ("Artist Statement", "Contact") are larger with visible letter-spacing
    6. Navigate to /contact — verify "Get in Touch" heading is dramatically large with wide letter-spacing
    7. In Chrome DevTools, toggle "Emulate CSS media feature prefers-reduced-motion" to "reduce", refresh — grain should be static (no animation)
    8. Verify the intro skips immediately on refresh (localStorage remembered)
  </how-to-verify>
  <verify>Human confirms visual appearance matches atmospheric intent</verify>
  <done>Human approves the atmospheric look across all pages</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. globals.css contains `--tracking-brand-tight`, `--tracking-brand-wide`, `--tracking-brand-widest` in @theme
3. IntroAnimation.tsx h1 has `tracking-brand-wide` and `md:text-8xl`
4. contact/page.tsx h1 has `tracking-brand-wide` and `md:text-7xl`
5. about/page.tsx h2 elements and RichText `[&_h2]:` chain include `tracking-brand-wide`
6. Human verifies visual appearance across all three pages
</verification>

<success_criteria>
- Typography is visibly more commanding than v1.0 — wider letter-spacing, larger size contrast
- Heading-to-body size ratio exceeds 3:1 on all pages
- Strategic italic only on secondary/subtitle elements
- RichText headings match direct heading styling
- Human approves overall atmospheric look (grain + vignette + typography together)
</success_criteria>

<output>
After completion, create `.planning/phases/04-atmospheric-foundation/04-02-SUMMARY.md`
</output>
